[pytest]
pythonpath = .
testpaths = tests
markers =
    smoke: critical tests that verify core application functionality
    regression: tests used for broader regression coverage
    negative: tests that verify invalid or error scenarios
    e2e: end-to-end user journey tests